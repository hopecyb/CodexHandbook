---
title: Skill 구조
description: SKILL.md, references/, templates/, scripts/의 역할과 예시.
locale: ko
source_locale: zh-CN
source_revision: 5f36443
translation_status: draft
translated_at: 2026-07-28
---

Skill 디렉터리를 처음 볼 때 흔한 느낌은 「폴더는 다 아는데, 왜 이렇게 나눠 두는지 모르겠다」입니다.

이 페이지는 「주 절차, 긴 설명, 템플릿, 스크립트」를 어떻게 나눠 두어 한 파일에 전부 넣지 않는지 설명합니다.

읽기·수정·재사용이 쉬워지고, 갈수록 어수선해지는 것도 줄일 수 있습니다.

# Skill 구조

## 디렉터리 구조

```text
my-skill/
├── SKILL.md          # 필수
├── scripts/          # 선택: 결정적 단계
├── references/       # 선택: 긴 참고 문서
└── templates/        # 선택: 출력 템플릿
```

## 각 층의 역할

- `SKILL.md`: 주 설명서. Codex에 「무엇인지, 언제 쓰는지, 어떤 단계로 하는지」를 먼저 알림
- `references/`: 긴 자료함. 모든 배경을 주 파일에 억지로 넣지 않음
- `templates/`: 바로 쓸 출력 골격. 매번 즉흥으로 만들지 않음
- `scripts/`: 결정적으로 실행할 단계는 스크립트에 맡김. 전부 자연어로만 쓰지 않음

처음에는 `SKILL.md` 하나만으로도 충분합니다.  
나중에 정말 복잡해지면 `references/`, `templates/`, `scripts/`를 분리하세요.

## SKILL.md 최소 예시

```md
---
name: pr-review
description: 현재 브랜치의 main 대비 diff를 검토하고 위험과 테스트 공백을 표시합니다. 사용자가 review, PR 검토, 병합 전 검사를 요청할 때 사용합니다.
---

## 단계
1. main 대비 diff 가져오기
2. 파일별 분류: 논리 오류, 보안, 성능, 테스트
3. 등급별 목록 출력: 차단 / 제안 / nit
4. 자동 push나 병합은 하지 않음
```

## 흔한 오해

### 1. 처음부터 모든 디렉터리를 다 만들어야 한다

좋은 Skill 중 많은 것은 처음에 `SKILL.md`만 있고, 재사용이 늘면서 다른 디렉터리를 서서히 분리합니다.

### 2. `references/`는 내용을 더 넣기 위한 것이다

핵심은 「더 넣는」 것이 아니라, 주 절차의 주의를 항상 차지할 필요가 없는 긴 설명을 필요할 때만 읽게 두는 것입니다.

### 3. `scripts/`는 가능하면 쓰지 않는다

어떤 단계가 원래 안정·결정·반복 가능해야 한다면, 계속 자연어로 설명하는 것보다 스크립트로 쓰는 편이 더 신뢰할 수 있습니다.

## 각 디렉터리 역할

| 디렉터리 | 용도 |
|---|---|
| `SKILL.md` | 주 절차, 트리거 조건, 금지 항목 |
| `references/` | 긴 문서, 규범, API 설명 |
| `templates/` | 출력 형식, 보고서 골격 |
| `scripts/` | 결정적 명령과 검사 |

## 언제 디렉터리를 나눌까

다음 순서로 나눌지 판단하세요.

1. 주 절차가 이미 너무 길어 처음 읽기 어려운가
2. 매번 볼 필요 없는 큰 자료 덩어리가 있는가
3. 고정 출력 형식을 계속 반복하는가
4. 어떤 단계가 스크립트화할 만큼 안정되었는가

이 네 가지 중 하나둘에 「예」라면 분리를 시작해도 됩니다.

먼저 `SKILL.md`로 통과시킨 뒤, 정말 복잡해질 때 자료·템플릿·스크립트를 단계적으로 빼내세요.

전체 실습: [첫 Skill 만들기](/skills/create-your-first-skill/).
## 템플릿에서 Skill로

많은 팀은 처음부터 Skill을 설계하지 않습니다. 반복해서 복사하던 프롬프트에서 시작합니다. 다음 단계로 발전시킬 수 있습니다.

| 단계 | 형태 | 언제 올릴까 |
|---|---|---|
| 일회성 프롬프트 | 현재 대화 | 한 번만 사용 |
| 템플릿 | 프롬프트 모음 또는 팀 문서 | 비슷한 작업은 반복되지만 단계는 아직 바뀜 |
| 단일 파일 Skill | `SKILL.md` 하나 | 절차가 안정되고 트리거가 명확함 |
| 완전한 Skill 디렉터리 | `SKILL.md` + `templates/` + `references/` + `scripts/` | 출력 형식, 긴 참고자료, 결정적 검사가 안정됨 |

모든 템플릿을 Skill로 바꿀 필요는 없습니다. 반복 사용, 명확한 성공 기준, 팀 합의가 있는 것만 남기세요.

## 재사용 가능한 Skill 구성 요소

| 구성 요소 | 적합한 내용 |
|---|---|
| 주 설명 | 트리거, 단계, 금지 사항, 완료 정의 |
| 템플릿 | PR 리뷰 출력, 문서 구조, 위험 보고서 |
| 참고자료 | 코드 규칙, 용어집, 도메인 규칙, API 설명 |
| 스크립트 | 복잡도 분석, 링크 검사, 커버리지 집계, 포맷 검사 |

좋은 Skill은 더 긴 프롬프트가 아니라, 재사용 과정을 읽기 쉽고 테스트 가능하며 유지보수 가능한 부분으로 나눈 것입니다.

---

**상태:** verified  
**적용 제품:** App / CLI / IDE / Cloud  
**검증 근거:** 현재 Codex 런타임 스킬 설명은 여전히 `SKILL.md`를 핵심 파일로 쓰며, 필요 시 스크립트와 참고 자료를 확장할 수 있습니다. 이 페이지는 디렉터리 역할 구분에 초점을 두며, 자주 바뀌는 제품 입구에 묶이지 않습니다.  
**최근 검증:** 2026-07-26
