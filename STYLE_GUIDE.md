# Codex Handbook 写作与视觉规范

本规范适用于简体中文正文、示例、图示和规划文档。简体中文是源语言；其他语言在源文稳定后再同步。

## 1. 先明确页面职责

每个页面只承担一种主要职责：

| 页面类型 | 主要回答 | 不应变成 |
|---|---|---|
| 学习路径 | 先学什么、后学什么 | 重复整篇教程 |
| 产品指南 | 在哪里操作、入口有何差异 | 通用方法论合集 |
| 能力专题 | 为什么这样做、边界是什么 | 某个界面的逐按钮截图 |
| 工作流 | 怎样稳定重复完成 | 只适用于一次任务的记录 |
| 案例 | 真实任务怎样走完闭环 | 只有结果、没有过程的展示 |
| 参考资料 | 快速查到准确答案 | 连续教学长文 |

新页面动笔前，先写一句“读完后，读者能够……”。如果这句话包含两个不相关目标，应拆页或缩小范围。

## 2. 从读者问题开始

- 默认读者不熟悉 Codex、终端、Git、Agent 和自动化。
- 专业术语第一次出现时，用一句日常语言解释，再给正式名称。
- 先说明这个知识解决什么问题，再解释机制。
- 不用“显然”“只需”“非常简单”等替读者判断难度的表达。
- 产品名、配置键、命令、文件名和代码标识符保持官方写法。

推荐开头结构：

```markdown
这页解决什么问题。

## 适合谁

## 学完能做什么
```

短页面不必机械套模板，但必须让读者知道用途和下一步。

## 3. 内容由浅入深

重要教学页优先按以下顺序组织：

1. 通俗解释。
2. 最小可用示例。
3. 完整任务示例。
4. 错误示例与改写。
5. 验证方式。
6. 失败恢复与安全边界。
7. 可复制清单或模板。
8. 前后章节链接。

不要只给一段“万能提示词”。示例要同时交代输入、允许修改范围、预期产物和检查方法。

## 4. 中文表达

- 使用简体中文和全角中文标点。
- 中英文、中文与数字之间保留一个半角空格，例如“运行 `pnpm build` 后检查”。
- 代码、命令、路径和变量使用反引号。
- 英文缩写首次出现时写成“模型上下文协议（MCP）”。
- 标题用陈述句或读者问题，避免“赋能”“最佳实践全解析”等宣传式标题。
- 一段只讲一个中心意思；超过 5 行时考虑拆段。
- 避免连续使用“首先、其次、最后”和没有信息量的总结句。

术语以 [`glossary/terms.yml`](glossary/terms.yml) 为准。产品界面名称与术语表冲突时，保留官方名称并补充解释。

## 5. 示例与代码

示例必须满足：

- 路径、命令和预期输出相互一致。
- 密钥使用 `YOUR_API_KEY` 等明显假值。
- 能运行的示例给出准确运行命令。
- 故意失败的示例明确标注“预期失败”及其意义。
- 不用 `path/to/file` 冒充已经可运行的完整案例。
- 输出只保留理解结果所需的关键行。
- 危险或不可逆操作必须先给出风险和人工确认点。

完整案例应尽量包含 `brief`、起始材料、提示词、参考答案和验证说明，并放在 `examples/complete-workflows/`。

## 6. 事实、来源与状态

涉及当前产品行为、套餐、模型、命令、配置、权限或平台支持时：

1. 优先查 OpenAI 官方文档。
2. 在正文附近链接到支持该结论的具体页面。
3. 页尾记录状态、适用产品、核验依据和核验日期。
4. 没有足够证据时使用 `review` 或 `outdated`，不能标为 `verified`。

稳定方法论也要验证站内链接和示例，但不必伪造官方依据。

## 7. 图示

每张图必须解释正文中的一种关系：流程、层级、选择、状态变化、组件关系或对比。纯装饰图不计入教学图示。

- 站点图示放在 `public/diagrams/`，可编辑源稿放在 `docs/visual-sources/`。
- 文件名使用有意义的英文 kebab-case，并以语言后缀结尾，如 `task-execution-loop-zh-cn.svg`。
- SVG 必须包含 `<title>`、`<desc>`、`role="img"` 和稳定 `viewBox`。
- 正文必须提供准确 alt 文本；复杂图再补一段文字说明，不能让关键信息只存在于图片里。
- 连线先绘制、节点后绘制，避免箭头穿过半透明节点。
- 正文字号在原始 viewBox 中不小于 12 px；移动端仍需可辨认。
- 中文字体使用系统字体栈，不依赖远程字体才能阅读。
- 不使用远程热链，不提交无来源或版权不明的素材。

新增或替换图示后，更新 [`docs/planning/visual-assets-manifest.md`](docs/planning/visual-assets-manifest.md)。

## 8. 链接与文件

- 站内正文链接使用根路径并保留末尾 `/`，例如 `/guide/verification/`。
- 仓库文档链接使用相对路径。
- 一个概念只保留一个权威页面；兼容入口只写摘要并链接到权威页。
- 不创建空页面、孤立图片或没有导航入口的正式章节。
- 不直接修改 `dist/`、`.astro/` 和自动生成的 `src/content/docs/zh-tw/`。

## 9. 发布前检查

- 页面目的和目标读者清楚。
- 示例的输入、范围、输出和验证齐全。
- 易变事实有官方依据和日期。
- 内链与图片路径存在。
- 图示在桌面端和移动端无裁切、重叠、乱码。
- `pnpm build` 在 Node.js `>=22.12.0` 下通过。
- `git diff` 中没有生成目录、密钥或无关格式化改动。

