# 修复与验证

```text
目标：修复 cart.js 的折扣百分比换算错误。
输入：brief.md、cart.js、cart.test.js，以及刚才的失败测试证据。
范围：只修改 cart.js 中的根因；不改函数签名、测试断言和目录结构；不加依赖。
验收：同一个 cart.test.js 的 3 个测试全部通过，并展示测试摘要和最小 diff。
权限：不要提交或推送 Git；需要扩大范围时先停下说明。

先用一句话重述根因，再实施并运行测试。
```

